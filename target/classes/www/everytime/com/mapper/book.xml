<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bookns">
	<insert id="insert" parameterType="book">
		insert into booklist (isbn, title, authors, publisher, price, translators,
		datetime, thumbnail)
		values (#{isbn}, #{title}, #{authors}, #{publisher}, #{price},
		#{translators}, #{datetime}, #{thumbnail})
	</insert>

	<select id="selectList" parameterType="book" resultType="book">
		select title, authors, publisher, price, translators from booklist where
		isbn = #{isbn} limit 0,1;
	</select>
</mapper>
